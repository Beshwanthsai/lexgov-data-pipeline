# lexgov-data-pipeline
LexGov Data Pipeline is a preprocessing engine for extracting, cleaning, and structuring legal documents from official sources such as India Code. The pipeline converts raw legal PDFs into structured JSON format, enabling semantic search and Retrieval-Augmented Generation (RAG) systems.
use pymupdf package from fitz module in python to extract the text from pdf.
